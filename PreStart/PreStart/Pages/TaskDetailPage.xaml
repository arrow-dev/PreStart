<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PreStart;assembly=PreStart"
             x:Class="PreStart.Pages.TaskDetailPage">
 <StackLayout Padding="{x:Static local:Template.StackLayoutSection}">
 <StackLayout Orientation="Horizontal">
   <Label Text="Edit  Description" ></Label>
   <Button HorizontalOptions="Center" Text="Edit" TextColor="White" BackgroundColor="Blue"  VerticalOptions="Center"></Button>
   <Editor Text="{Binding Task.Description, Mode=TwoWay}" IsVisible="False"  ></Editor>
 </StackLayout>
   <StackLayout Orientation="Horizontal">
   <Label Text="Hazards"></Label>
     <Button Text="+" VerticalOptions="Center"></Button>
   </StackLayout>
   <ListView ItemsSource="{Binding Hazards}" RowHeight="50">
     <ListView.ItemTemplate>
       <DataTemplate>
         <ViewCell>
           <StackLayout Orientation="Horizontal">
             <Label Text="{Binding Description}"></Label>
             
           </StackLayout>
         </ViewCell>
       </DataTemplate>
     </ListView.ItemTemplate>
   </ListView>
   
 </StackLayout>
  </ContentPage>